# language: zh-CN
功能: Bug修复验证
  作为开发者
  我需要验证已修复的关键Bug不再复现
  以确保系统可靠性

  场景: 交易时间检查使用中国标准时间
    假如 服务器时区可能不是UTC+8
    当 我检查交易时间
    那么 使用的是CST时区而非服务器本地时区

  场景: AI评分端点有限流保护
    假如 AI评分端点存在
    当 我创建RateLimiter实例
    那么 限流器配置正确且能拒绝超额请求

  场景: Sina降级时缺失字段筛选被跳过
    假如 行情数据来自新浪备用源，缺失换手率/PE/PB/市值字段
    当 我按换手率或市盈率筛选
    那么 这些筛选条件被跳过，结果包含warning提示

  场景: sector_failed为None时不过滤全部股票
    假如 行业成分股API全部失败返回None
    当 我按行业筛选股票
    那么 sector_failed为True且返回warning

  场景: ETF交易价格可被查找
    假如 持仓中有ETF代码510300
    当 构建价格映射时
    那么 ETF价格被包含在映射中

  场景: RateLimiter超限后自动清理内存
    假如 RateLimiter中有超过256个IP条目
    当 有新的请求进来
    那么 空列表IP被清理，总条目不超过1024
