# language: zh-CN
功能: K线数据获取
  作为投资者
  我想查看股票的历史K线
  以便分析价格走势

  场景: 获取日K线数据
    假如 股票000001有日K线数据
    当 我请求日K线历史
    那么 返回的每条数据包含 day、open、high、low、close、volume 字段
    而且 所有字段值为字符串类型

  场景: 获取月K线数据
    假如 股票000001有历史数据
    当 我请求月K线历史
    那么 直接返回月K数据，无需手动聚合

  场景: 股票不存在时返回空列表
    假如 股票999999没有K线数据
    当 我请求该股票的K线历史
    那么 返回空列表
