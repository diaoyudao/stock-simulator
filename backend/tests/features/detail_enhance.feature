# language: zh-CN
功能: 股票详情页增强功能
  作为投资者
  我想在详情页查看分时图、五档盘口、资金流向和分钟K线
  以便更全面地分析个股走势和资金动向

  # ─── 分时图 ───

  场景: 获取个股分时数据
    假如 股票000001有分时成交数据
    当 我请求该股票的分时数据
    那么 返回当日每分钟成交数据列表
    而且 每条数据包含时间、成交价、成交量、成交性质

  场景: 非交易时间获取分时数据返回上一交易日数据
    假如 股票000001有分时成交数据
    而且 当前非交易时间
    当 我请求该股票的分时数据
    那么 返回最近交易日的分时数据

  场景: 不存在的股票分时数据返回空列表
    假如 股票999999不存在
    当 我请求不存在的股票的分时数据
    那么 分时返回空列表

  # ─── 五档盘口 ───

  场景: 获取个股五档盘口
    假如 股票000001有盘口数据
    当 我请求该股票的五档盘口
    那么 返回买1至买5和卖1至卖5的价格与挂单量
    而且 返回最新价、均价、涨停价、跌停价

  场景: 不存在的股票五档盘口返回空数据
    假如 股票999999不存在
    当 我请求不存在的股票的五档盘口
    那么 返回空字典

  # ─── 资金流向 ───

  场景: 获取个股资金流向
    假如 股票000001有资金流向数据
    当 我请求该股票的资金流向
    那么 返回近期每日资金流向列表
    而且 每条数据包含日期、主力净流入净额及净占比、超大单/大单/中单/小单分类

  场景: 不存在的股票资金流向返回空列表
    假如 股票999999不存在
    当 我请求不存在的股票的资金流向
    那么 资金流向返回空列表

  # ─── 分钟K线 ───

  场景: 获取5分钟K线数据
    假如 股票000001有分钟K线数据
    当 我请求该股票的5分钟K线
    那么 返回近期5分钟K线数据列表
    而且 每条数据包含时间、开盘价、最高价、最低价、收盘价、成交量

  场景: 不存在的股票分钟K线返回空列表
    假如 股票999999不存在
    当 我请求不存在的股票的5分钟K线
    那么 分钟K线返回空列表
