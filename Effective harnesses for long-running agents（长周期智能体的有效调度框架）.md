# Effective harnesses for long-running agents（长周期智能体的有效调度框架）

Effective harnesses for long-running agents（长周期智能体的有效调度框架）是Anthropic在2025年提出的工程化方案，核心是通过**双智能体结构+结构化状态持久化+增量式开发**，解决长任务中“上下文遗忘、状态丢失、提前完工”三大痛点。以下从核心挑战、架构设计、关键机制、实施流程与最佳实践展开，附可直接复用的模板与示例。

---

## 一、长周期智能体的核心挑战

长周期任务（小时/天级）中，智能体跨会话工作时面临三类致命问题：

1. **上下文腐化（Context Rot）**：单窗口token限制导致遗忘原始目标、忽略关键指令。

2. **状态断裂（State Amnesia）**：无持久化机制，中断后丢失所有进度，只能从头执行。

3. **执行失控**：
        

    - 一次性贪多（One-shot）：试图单会话完成全量功能，中途上下文溢出，留下半完成、无文档的烂摊子。

    - 提前完工（False Completion）：看到部分功能完成就误判任务结束。

    - 虚假验收：未端到端测试就标记功能完成，遗留深层bug。

---

## 二、核心架构：双智能体+结构化环境

Anthropic提出**初始化智能体（Initializer Agent）+编码智能体（Coding Agent）** 的双角色架构，配合文件系统与Git实现状态持久化，彻底隔离“环境初始化”与“增量执行”。

### 1. 初始化智能体（首次会话执行）

专属prompt驱动，仅运行一次，输出4个核心工件，构建“可恢复、可追溯、可验证”的项目基座：

- **feature_list.json**：结构化需求清单，将原始prompt拆解为200+可验收的原子功能，初始状态均标记为`"passes": false`，禁止修改结构，仅更新状态。

```json
{
  "category": "functional",
  "description": "新聊天按钮创建对话",
  "steps": ["点击按钮", "验证新对话", "检查欢迎页", "侧边栏可见"],
  "passes": false
}
```

- **claude-progress.txt**：进度日志，记录已完成步骤、当前状态、风险与待办，作为新会话的“快速入口”。

- **init.sh**：环境启动脚本，一键拉起开发服务器、执行基础健康检查，避免每次会话重复配置。

- **Git初始提交**：提交空项目骨架与上述文件，建立版本基线，支持回滚与追溯。

### 2. 编码智能体（后续所有会话）

复用同一模型与工具集，专属prompt聚焦**增量开发+状态维护+严格验收**，会话流程标准化：

1. 环境校准：执行`pwd`→读取`claude-progress.txt`→读取`feature_list.json`→查看Git日志→运行`init.sh`验证基础功能，快速对齐项目状态。

2. 单功能聚焦：仅选择1个未完成的最高优先级功能开发，杜绝贪多。

3. 增量实现：编码→单元测试→端到端验证（如用Puppeteer自动化浏览器），**必须通过验收才标记`passes": true`**。

4. 状态固化：Git提交（规范信息）→更新`claude-progress.txt`→清理环境，确保下次会话可无缝接续。

---

## 三、四大关键机制（解决核心痛点）

### 1. 外部状态替代上下文（External Memory）

- 摒弃“依赖上下文窗口记忆”，改用**文件系统+Git**作为持久化层，每次会话“重载世界”而非依赖记忆。

- 核心状态文件：**feature_list.json（需求源）、claude-progress.txt（进度源）、Git（版本源）**，三者构成“不可篡改的事实源”。

### 2. 增量式开发与严格验收

- **小步快跑**：单会话只做1个功能，降低上下文溢出风险，提升可验证性。

- **验收铁律**：禁止删除/修改测试用例；必须端到端测试（模拟用户操作）；仅在全量验证通过后更新状态。

### 3. 会话启动标准化（Bootstrap Routine）

每次会话固定执行5步，消除“启动混乱”，节省token：

```bash
# 1. 定位目录
pwd
# 2. 读取进度
cat claude-progress.txt
# 3. 读取需求
cat feature_list.json
# 4. 查看历史
git log --oneline -20
# 5. 环境健康检查
bash init.sh
```

### 4. 错误恢复与回滚

- Git作为“安全网”：每次提交都是可恢复的状态，出错时直接回滚到上一个稳定版本。

- 进度日志追溯：通过`claude-progress.txt`快速定位上次会话的终止点，避免重复工作。

---

## 四、实施流程（可直接落地）

### 阶段1：初始化（Initializer Agent）

1. 输入：用户高层需求（如“复刻claude.ai”）。

2. 输出：feature_list.json（200+功能）、claude-progress.txt（初始日志）、init.sh（启动脚本）、Git初始提交。

3. 验收：检查4个文件是否完整、格式正确、功能清单无遗漏。

### 阶段2：增量开发（Coding Agent，循环执行）

1. 启动校准：执行标准化启动流程，确认环境健康。

2. 功能选择：从feature_list.json中选1个最高优先级未完成功能。

3. 开发实现：编码→单元测试→端到端验证。

4. 状态更新：标记功能为`passes": true`→Git提交→更新进度日志。

5. 循环：重复直至所有功能完成。

### 阶段3：收尾与交付

1. 全量回归测试：验证所有功能正常。

2. 清理环境：确保代码可合并到主分支，无bug、文档完整。

3. 最终提交：Git合并，归档所有状态文件。

---

## 五、最佳实践（行业验证有效）

1. **强结构化工件**：优先用JSON存储需求与状态（模型不易误改），避免自由文本。

2. **会话短而频**：单会话控制在30-60分钟，减少上下文累积，提升稳定性。

3. **工具链标准化**：内置浏览器自动化（Puppeteer）、Git、文件操作工具，统一执行环境。

4. **分层抽象替代压缩**：用“主Agent（规划+摘要）+子Agent（详细执行）”替代上下文压缩，无损保留细节，突破单窗口限制。

5. **人类评审节点**：关键里程碑引入人工验收，避免智能体自我验证的盲区。

---

## 六、典型失败模式与解决方案对照表

|问题|初始化智能体对策|编码智能体对策|
|---|---|---|
|提前完工|生成完整功能清单，标记所有为未完成|会话开始读取清单，仅做1个功能|
|状态混乱/无文档|创建Git仓库与进度日志|会话结束提交代码+更新日志|
|虚假验收|功能清单包含端到端步骤|必须完成所有步骤才标记通过|
|环境配置耗时|编写init.sh一键启动脚本|会话开始执行脚本验证环境|
---

## 七、总结

长周期智能体的有效调度框架，本质是**把软件工程的最佳实践（版本控制、增量开发、严格验收、文档化）转化为智能体可执行的规则与工件**，用“结构化环境+双角色分工+外部状态持久化”，替代“依赖模型记忆的脆弱设计”，最终实现跨小时/天级任务的稳定、可追溯、可恢复执行。
> （注：文档部分内容可能由 AI 生成）